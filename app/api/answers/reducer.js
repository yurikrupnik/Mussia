import {SOME_ACTION_TYPE} from './actions';

// To Be Added
export default (state = { data: [], active: false }, action) => {
    switch (action.type) {
        default:
            return state;
    }
};